% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_htna_diff.R
\name{plot_htna_diff}
\alias{plot_htna_diff}
\title{Plot the Difference Between Two htna Networks}
\usage{
plot_htna_diff(x, y, ...)
}
\arguments{
\item{x, y}{htna networks produced by \code{\link[=build_htna]{build_htna()}} with matching
actor partitions.}

\item{...}{Forwarded to \code{\link[cograph:plot_htna]{cograph::plot_htna()}}. User args win.}
}
\value{
The value returned by \code{\link[cograph:plot_htna]{cograph::plot_htna()}} (invisibly).
}
\description{
Renders the elementwise edge-weight difference \code{x - y} as a heterogeneous
transition network using \code{\link[=plot_htna]{plot_htna()}}. Both networks must share the same
actor partition (same group labels in \verb{$node_groups}).
}
\details{
This is the htna-side workaround for \code{cograph::plot_compare()}, which does
not (yet) accept a \code{node_list} or pre-computed multi-group layout. As a
result, the signed (positive / negative) edge coloring that \code{plot_compare}
provides upstream is lost here - edges are colored as in any other
\code{\link[=plot_htna]{plot_htna()}} call. When cograph adds \code{node_list} (or a layout-matrix
argument) to \code{plot_compare}, swap this implementation for a real
\code{plot_compare} wrapper that preserves diff semantics.
}
\examples{
\dontrun{
data(human_long, ai_long, package = "Nestimate")
net1 <- build_htna(list(Human = human_long,        AI = ai_long))
net2 <- build_htna(list(Human = human_long[1:50,], AI = ai_long))
plot_htna_diff(net1, net2)
}

}
\seealso{
\code{\link[=plot_htna]{plot_htna()}}, \code{\link[=build_htna]{build_htna()}}.
}
