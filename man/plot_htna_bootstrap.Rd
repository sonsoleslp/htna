% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_htna_bootstrap.R
\name{plot_htna_bootstrap}
\alias{plot_htna_bootstrap}
\title{Plot an htna Bootstrap Result}
\usage{
plot_htna_bootstrap(boot, ...)
}
\arguments{
\item{boot}{An object returned by \code{\link[=bootstrap_htna]{bootstrap_htna()}}.}

\item{...}{Forwarded to \code{\link[cograph:splot]{cograph::splot.net_bootstrap()}}. User args win
over the htna defaults.}
}
\value{
The value returned by \code{\link[cograph:splot]{cograph::splot.net_bootstrap()}} (invisibly).
}
\description{
Composes htna's visual defaults and the htna multi-group circular layout
with cograph's bootstrap renderer (\code{\link[cograph:splot]{cograph::splot.net_bootstrap()}}). The
CI / significance overlay is whatever cograph produces; htna only owns the
geometry and the visual identity (so the bootstrap plot looks like the
same network as \code{\link[=plot_htna]{plot_htna()}}).
}
\details{
The layout is computed on the htna side and injected as a matrix together
with \code{rescale = FALSE} and \code{layout_scale = 1}. Those three arguments
together are the contract that lets the htna geometry survive into
cograph's renderer unchanged - drop any one and cograph re-normalizes.
}
\examples{
\dontrun{
data(human_long, ai_long, package = "Nestimate")
net  <- build_htna(list(Human = human_long, AI = ai_long))
boot <- bootstrap_htna(net, iter = 200)
plot_htna_bootstrap(boot)
plot_htna_bootstrap(boot, display = "significant")
}

}
\seealso{
\code{\link[=bootstrap_htna]{bootstrap_htna()}}, \code{\link[=plot_htna]{plot_htna()}}.
}
