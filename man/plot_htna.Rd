% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_htna.R, R/reexports.R
\name{plot_htna}
\alias{plot_htna}
\title{Plot a Heterogeneous Transition Network}
\usage{
plot_htna(x, layout = "circular", group_colors = htna_palette, ...)

plot_htna(x, layout = "circular", group_colors = htna_palette, ...)
}
\arguments{
\item{x}{A network object produced by \code{\link[=build_htna]{build_htna()}}.}

\item{layout}{Character. Layout algorithm. Default \code{"circular"}.}

\item{group_colors}{Character vector of colours, one per actor group.
Defaults to the built-in \link{htna_palette}.}

\item{...}{Additional arguments passed to \code{\link[cograph:plot_htna]{cograph::plot_htna()}}.}
}
\value{
The value returned by \code{\link[cograph:plot_htna]{cograph::plot_htna()}} (invisibly).

Called for its side effect (a plot). Returns \code{x} invisibly.
}
\description{
htna's wrapper around \code{\link[cograph:plot_htna]{cograph::plot_htna()}}. Reads the actor partition
from the canonical \verb{$node_groups} schema written by \code{\link[=build_htna]{build_htna()}},
applies the htna visual defaults, and forwards everything to
\code{\link[cograph:plot_htna]{cograph::plot_htna()}} with \code{node_list} passed explicitly (no reliance on
cograph's column-name auto-detection).

Wrapper around \code{\link[cograph:plot_htna]{cograph::plot_htna()}} with defaults suited for HTNA
networks built by \code{\link[=build_htna]{build_htna()}}. When \code{layout = "circular"}, a custom
layout is computed so that the first actor group appears on the left.
}
\details{
User arguments in \code{...} always win over the defaults.
}
\examples{
\dontrun{
data(human_long, ai_long, package = "Nestimate")
net <- build_htna(list(Human = human_long, AI = ai_long))
plot_htna(net)
plot_htna(net, threshold = 0.1, legend = FALSE)
}

\dontrun{
data(human_long, ai_long, package = "Nestimate")
net <- build_htna(list(Human = human_long, AI = ai_long))
plot_htna(net)
plot_htna(net, layout = "auto", threshold = 0.1)
}

}
\seealso{
\code{\link[=build_htna]{build_htna()}}, \code{\link[=plot_htna_bootstrap]{plot_htna_bootstrap()}}, \code{\link[=plot_htna_diff]{plot_htna_diff()}}.

\code{\link[cograph:plot_htna]{cograph::plot_htna()}}, \code{\link[=build_htna]{build_htna()}}
}
