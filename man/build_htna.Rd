% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_htna.R
\name{build_htna}
\alias{build_htna}
\title{Build a Heterogeneous Transition Network (HTNA)}
\usage{
build_htna(
  data,
  actor_col = NULL,
  node_groups = NULL,
  action = "code",
  session = "session_id",
  order = "order_in_session",
  method = "relative",
  group = NULL,
  disambiguate = FALSE,
  ...
)
}
\arguments{
\item{data}{Either:
\itemize{
\item A named list of long-format data frames, one per actor
(e.g. \code{list(Human = human_long, AI = ai_long)}). All frames
must share the same column schema.
\item A single long-format data frame. In that case either
\code{actor_col} (row-level actor IDs) or \code{node_groups}
(node-level actor lookup) must be supplied.
}}

\item{actor_col}{Character. Name of the actor column when \code{data} is
a single data frame and actor identity lives at the row level. Ignored
when \code{data} is a named list or when \code{node_groups} is supplied.}

\item{node_groups}{Named list mapping actor labels to character vectors of
code names, e.g. \code{list(Human = c("Specify", "Command"),
  AI = c("Plan", "Execute"))}. Use this when \code{data} is a single
long-format frame with no actor column and you want to declare the
node-to-actor partition directly. Each code in \code{data[[action]]}
must appear in exactly one of the vectors. Mutually exclusive with
\code{actor_col}.}

\item{action}{Character. Name of the action/code column. Default
\code{"code"}.}

\item{session}{Character. Name of the session column. Default
\code{"session_id"}.}

\item{order}{Character. Name of the within-session order column. Default
\code{"order_in_session"}.}

\item{method}{Character. Transition method passed to
\code{\link[Nestimate]{build_network}}: \code{"relative"} (default),
\code{"frequency"}, or \code{"attention"}.}

\item{group}{Character. Optional name of a column in \code{data} used to
split sessions into cohorts (e.g. \code{"cluster"}). When supplied,
one network is built per group level and the result is a named list of
htna networks with class \code{c("htna_group", "netobject_group")}.}

\item{disambiguate}{Logical. If \code{FALSE} (default), the function errors
when a code label appears in more than one actor group. If \code{TRUE},
codes are prefixed with the actor name (\code{"Human:Ask"},
\code{"AI:Ask"}) so they become distinct nodes.}

\item{...}{Additional arguments forwarded to
\code{\link[Nestimate]{build_network}}.}
}
\value{
A \code{netobject} with the actor partition stored in cograph's
canonical schema, so \code{cograph::plot_htna(net)} auto-detects the
groups with no further arguments:
\itemize{
\item \code{$nodes$groups} - actor label per node (the column name
\code{cograph::plot_htna} auto-detects).
\item \code{$node_groups} - data frame with columns \code{node} and
\code{group} (canonical \code{cograph_network} schema, also
readable by \code{cograph::get_groups}, \code{cluster_summary}, and
the \code{print} method for \code{cograph_network}).
}
All other slots are exactly as returned by
\code{\link[Nestimate]{build_network}}.
}
\description{
Builds a transition network over a combined sequence of two or more actor
groups (e.g. Human and AI) and preserves the actor partition on the result
so downstream plotting and analysis can treat each actor's codes as a
distinct node group.
}
\examples{
\dontrun{
data(human_long, ai_long, package = "Nestimate")
net <- build_htna(list(Human = human_long, AI = ai_long))
net$node_groups            # canonical (node, group) schema
cograph::plot_htna(net)    # auto-detects $nodes$groups, no other args
}

}
\seealso{
\code{\link[Nestimate]{build_network}},
\code{\link[Nestimate]{build_tna}},
\code{\link[cograph]{plot_htna}}
}
