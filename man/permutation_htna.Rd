% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation_htna.R
\name{permutation_htna}
\alias{permutation_htna}
\title{Permutation Test for Network Differences}
\usage{
permutation_htna(
  x,
  y = NULL,
  iter = 1000L,
  alpha = 0.05,
  paired = FALSE,
  adjust = "none",
  nlambda = 50L,
  seed = NULL
)
}
\arguments{
\item{x}{A \code{netobject} (from \code{\link[Nestimate]{build_network}}).}

\item{y}{A \code{netobject} (from \code{\link[Nestimate]{build_network}}).
Must use the same method and have the same nodes as \code{x}.}

\item{iter}{Integer. Number of permutation iterations (default: 1000).}

\item{alpha}{Numeric. Significance level (default: 0.05).}

\item{paired}{Logical. If \code{TRUE}, permute within pairs (requires
equal number of observations in \code{x} and \code{y}). Default: FALSE.}

\item{adjust}{Character. p-value adjustment method passed to
\code{\link[stats]{p.adjust}} (default: \code{"none"}). Common choices:
\code{"holm"}, \code{"BH"}, \code{"bonferroni"}.}

\item{nlambda}{Integer. Number of lambda values for the \code{glassopath}
regularisation path (only used when \code{method = "glasso"}).
Higher values give finer lambda resolution at the cost of speed.
Default: 50.}

\item{seed}{Integer or NULL. RNG seed for reproducibility.}
}
\value{
An object of class \code{net_permutation} (single pair) or
\code{net_permutation_group} (multiple pairs). See
\code{\link[Nestimate:permutation]{Nestimate::permutation()}} for the full slot list.
}
\description{
htna-named alias of \code{\link[Nestimate:permutation]{Nestimate::permutation()}}. Tests whether
observed edge-weight differences between two networks (or all
pairwise differences within a \code{netobject_group}) exceed what would
be expected under a null of identical generating processes.
}
\details{
Works on htna networks: the actor partition (\verb{$node_groups},
\verb{$actor_levels}, \code{htna} class) is preserved on \code{result$x} /
\code{result$y}, so \code{\link[=plot_htna_diff]{plot_htna_diff()}} can render the result with
htna's colour and layout conventions.

Suffixed \verb{_htna} to avoid clashing with \code{Nestimate::permutation()}
when both packages are loaded.
}
\examples{
\donttest{
data(human_long, ai_long, package = "Nestimate")
n <- nrow(human_long)
early <- build_htna(list(Human = human_long[seq_len(n \%/\% 2), ],
                         AI    = ai_long[seq_len(n \%/\% 2), ]))
late  <- build_htna(list(Human = human_long[(n \%/\% 2 + 1):n, ],
                         AI    = ai_long[(n \%/\% 2 + 1):n, ]))
permutation_htna(early, late, iter = 100)
}
}
\seealso{
\code{\link[=plot_htna_diff]{plot_htna_diff()}} to plot the result.
}
